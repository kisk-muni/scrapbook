{
  "name": "importer",
  "version": "0.0.1",
  "private": true,
  "main": "src/main.ts",
  "dependencies": {
    "dotenv": "^16.0.3",
    "papaparse": "^5.4.1",
    "shared": "*",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.14",
    "ts-node": "^10.8.0",
    "tsconfig": "*",
    "typescript": "5.2.2"
  },
  "scripts": {
    "start": "ts-node-esm -T src/main.ts",
    "transpile": "tsc",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
  }
}
