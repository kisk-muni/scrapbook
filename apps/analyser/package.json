{
  "name": "analyser",
  "version": "0.0.1",
  "private": true,
  "main": "src/main.ts",
  "dependencies": {
    "dotenv": "^16.0.3",
    "openai": "^4.11.1",
    "shared": "*",
    "zod": "^3.22.4",
    "zod-to-ts": "^1.2.0"
  },
  "devDependencies": {
    "puppeteer": "^19.7.5",
    "ts-node": "^10.8.0",
    "tsconfig": "*",
    "typescript": "5.2.2"
  },
  "scripts": {
    "start": "npm run dev",
    "start:prod": "node dist/main.js",
    "dev": "ts-node-esm -T src/main.ts",
    "transpile": "tsc",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
  }
}
